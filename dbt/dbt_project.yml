name: analytics_platform
version: "1.0.0"
config-version: 2
profile: analytics_platform

model-paths: ["models"]

models:
  analytics_platform:
    staging:
      +materialized: view
    marts:
      +materialized: table
